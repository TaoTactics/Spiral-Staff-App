# Database (contains sensitive service user data)
*.db
*.db-shm
*.db-wal

# Uploaded files (may contain sensitive documents)
uploads/

# Dependencies
node_modules/

# Backup HTML files (contain sensitive data)
backups/

# Env vars (backup credentials)
.env

# Misc
package-lock.json
